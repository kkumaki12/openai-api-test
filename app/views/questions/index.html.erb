<h1>質問一覧</h1>

<% @questions&.each do |question| %>
  <h2><%= link_to question.title, question_path(question) %></h2>
  <p><%= truncate(question.content, length: 100) %></p>
<% end %>

<%= link_to "新規質問作成", new_question_path %>
